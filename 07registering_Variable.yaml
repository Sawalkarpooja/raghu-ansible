- name: Register a variable
  hosts: all
  connection: local
  tasks:
    - name: run a shell script
      ansible.builtin.shell: uptime
      register: out
    - name: print out
      ansible.builtin.debug:
        msg: "{{out}}"